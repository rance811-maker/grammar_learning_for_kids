name: Direct Deploy

on:
  push:
    branches: [deploy-fix-temp2]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm sound.js
        run: |
          echo '== sound.js new combo markers =='
          curl -s https://grammar-quest-pet.netlify.app/js/sound.js | grep -iE 'major-pentatonic|Sparkle on top|level-up' || echo 'OLD sound.js'
